# YYYY-MM-DD

> 当天第一次变更时，将此文件复制为 `Memory/YYYY-MM-DD.md`。
> 每次独立变更一条记录。当两者都适用时，同时记入 `Skills/Memory/`（系统变更）和 `Working-Journal/daily/`（执行的工作）。

## <技能变更 / 工作项 —— 短标题>

### 请求
- 被要求做什么（1–2 行）。

### 完成的工作
- 产出/变更了什么（文件、决策、产出物）。
- 创建/修改文件的路径。

### 下一步
- 待定决策、后续跟进或待解问题。