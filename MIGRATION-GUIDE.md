# 迁移指南 —— 采纳这套系统

如何把这份模板变成你自己的工作体系。三条路径，选适合你的那条。

## 路径 A —— 从零开始（推荐）

1. **复制骨架。** 把 `Skills/` 和 `Working-Journal/` 复制到你的工作目录（例如 `~/work/skills`）。保持它们是两个独立的 git 仓库——它们有不同的生命周期。
2. **填充分流目录。** 打开 `Skills/index.md`，把 EXAMPLE 行替换成*你的*技能。保留结构（表格 + 触发词映射 + 关系）；只改内容。
3. **写你的第一个技能。** 选一个你反复做过的任务——做过 3 次以上、之后还会做的。复制 `Skills/Working/_TEMPLATE.md` 到 `Skills/Working/<技能-id>.md` 并填写：
   - `id` —— kebab-case，**必须等于文件名**
   - `triggers` —— 3–9 条你（或 AI）在这个任务适用时会真实说出的话
   - `summary` —— 一句话
   - 正文 —— 选适合的版式（见 `_TEMPLATE.md` 中总结的六种版式）
4. **注册它。** 在 `index.md` 的技能表加一行，并在触发词映射中加一条。
5. **记录日志。** 在 `Skills/Memory/YYYY-MM-DD.md` 追加一行（今天没有就新建该文件）。这条规则是强制的——未来会话靠它知道改了什么。
6. **开始日志。** 为今天创建 `Working-Journal/daily/YYYY-MM-DD.md`；当第一个真实事件落地时创建 `active/` 文件夹。

## 路径 B —— 从现有笔记迁移

你已有零散笔记、markdown 文件，或一套做事的经验记忆。转换它们：

1. 把每个候选主题放进 `skill-creation-rubric.md` 打分（得分 ≥ 9 → 新建技能；5–8 → 扩展现有技能；< 5 → 放弃）。
2. 对每个技能提取：**何时适用**（→ 触发词）、**产出什么**（→ summary + Artifacts）、**步骤/判断**（→ 正文）、**以前哪里出过问题**（→ Gotchas）。
3. 最有价值的技能是判断流程，不是静态事实。"我如何决定这个票归谁"胜过"组织架构表"。写流程；查找表放在附录。
4. 旧的 incident 复盘会变成排障技能：规范化输入 → 证据优先工作流 → 决策矩阵 → 响应模板 → 踩坑。
5. 原始笔记归档到 `Working-Journal/Archive/<项目>/` 下，什么也不丢失。

## 路径 C —— 与 AI 模型配合使用（推荐工作流）

这套系统设计成模型可以自我路由：

1. **每天开工、第一次对话：** 模型读取 `Skills/index.md` → `Skills/.github/copilot-instructions.md` → 最新的 `Working-Journal/daily/`（外加 7 天回看，找出未完结主题）→ 最新的 `Skills/Memory/` 记录。
2. **工作期间：** 模型把你的措辞匹配到触发词映射，打开正确的技能文件，而不是即兴发挥。
3. **新的可复用知识：** 说"这应该是个技能"（或让模型通过 rubric 主动提议）→ 创建技能文件 → 更新 `index.md` → 记录记忆日志。
4. **想保留的工作产出：** 说"保留这个"或"归档这个" → 产出保存到 `Skills/Working/Artifacts/<技能-id>/` 下，并更新当日日志。

## 要遵守的约定（它们是系统的粘合剂）

| 约定 | 为什么重要 |
|---|---|
| `id` == 文件名 | 路由链接按文件名解析；破坏它，链接会悄悄失效。 |
| 触发词驱动路由 | 每个技能需要 3–9 条真实请求者会说的话。模糊的触发词 = 技能永远走不到。 |
| 每次改 Skills/ 都必须同步记忆 | 未来会话从记忆日志加载上下文。跳过它 = 失忆。 |
| 日志同时覆盖两个仓库 | 日志记*工作*，记忆记*系统变更*。开工时两者都读。 |
| 秘密永不进 `.md` 文件 | 订阅、密码、令牌、个人标识符绝不写进技能文件。只记录它们存在哪里（密码管理器），不记录值。 |
| 每个技能独立的 Artifacts 目录 | 生成的产出放到 `Working/Artifacts/<技能-id>/` 下，命名为 `<技能-id>-YYYY-MM-DD.md`。 |

## 常见错误

- **一个技能只做一件事。** 别写"云上所有事"这种大杂烩技能；按工作流拆分。rubric 能防止这一点。
- **静态事实代替流程。** 一张没有分流逻辑的责任人表只是清单，不是技能。要写你如何决策。
- **跳过记忆同步。** 下周你就不会记得为什么有这条规则。当天就记。
- **公开仓库。** 这套系统会积累个人数据。除非你刻意做过脱敏，否则两个仓库都要保持私有。
- **过度标准化正文。** frontmatter 才是契约。如果 checklist 版式比叙述版式更适合 runbook，就用 checklist。